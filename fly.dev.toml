# fly.toml app configuration file generated for omnistrate-blog-dev on 2025-10-31T18:04:44+05:30
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'omnistrate-blog-dev'
primary_region = 'bom'
kill_signal = 'SIGINT'
kill_timeout = '5s'

[build]

[deploy]
  strategy = 'rolling'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'off'

[[services]]
  protocol = 'tcp'
  internal_port = 3000
  auto_stop_machines = 'off'

  [[services.ports]]
    port = 80
    handlers = ['http']
    force_https = true

  [[services.ports]]
    port = 443
    handlers = ['tls', 'http']

  [services.concurrency]
    type = 'requests'
    hard_limit = 30
    soft_limit = 20

  [[services.tcp_checks]]
    interval = '15s'
    timeout = '2s'
    grace_period = '2s'

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
